PORT=4001

# Firebase Admin — pick ONE:
# (A) Default: place Google service account JSON at backend/serviceAccountKey.json
# (B) Point to your downloaded key (recommended so the key never lives inside the repo):
# FIREBASE_SERVICE_ACCOUNT_PATH=/absolute/path/to/jira-mcp-server-18359-firebase-adminsdk-fbsvc-5fcad2b8df.json
# (C) CI / serverless: paste the entire JSON as a single line (escape quotes as needed):
# FIREBASE_SERVICE_ACCOUNT_JSON={"type":"service_account",...}
